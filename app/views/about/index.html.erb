<h1>about</h1>

<% @abouts.each do |about| %>
  <div class="card">
    <p>
    <%= about.description %>
    </p>
    <% if admin_signed_in? %>
      <%= link_to "Edit", edit_about_path(about) %>
    <% end %>
  </div>
<% end %>
